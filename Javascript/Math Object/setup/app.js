/**
 * Thema: JS Math Object
 *
 * Dokumentation:
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math
 * Deutsch: https://developer.mozilla.org/de/docs/Web/JavaScript/Reference/Global_Objects/Math
 *
 */

const number = 4.2;
// Gibt eine gerundete Zahl zurück 4.2 -> 4 und 4.6 -> 5
//const result = Math.round(number);

//Gibt eine AB-gerundete Zahl zurück
//const result = Math.floor(number);

//Gibt eine AUF-gerundete Zahl zurück
//const result = Math.ceil(number)

//Gibt eine Zufallszahl (Kommazahl) zwischen 0-1 zurück
// const result = Math.random();

//Gibt eine Zufallszahl (Kommazahl) zwischen 0-10 zurück
// const result = Math.random() * 10;

// ==> Ganze Zahl 0-9
// const result = Math.floor(Math.random() * 10);

//==> Ganze Zahl 1-10
//const result = Math.ceil(Math.random() * 10);

//==> Ganze Zahl 0-10 (0 und 10 kommen seltener vor)
//const result = Math.round(Math.random() * 10);

//Gibt die GRÖSSTE aller eingegebner Zahlen zurück
// const result = Math.max(4, 8, 15, 16, 23, 42);

//Gibt die KLEINSTE aller eingegebner Zahlen zurück
//const result = Math.min(4, 8, 15, 16, 23, 42);

console.log(result);
